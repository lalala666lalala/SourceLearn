# Copy to .env and fill in; .env is git-ignored and read by every run.
OPENAI_API_KEY=                     # backbone and judge on OpenAI, and the embedding model
OPENROUTER_API_KEY=                 # only for model ids of the form <vendor>/<model>
SOURCELEARN_REASONING_EFFORT=       # reasoning effort of the backbone
